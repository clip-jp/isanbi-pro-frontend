{
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "start": "node .output/server/index.mjs",
    "deploy": "push-dir --dir='.nuxt/dist' --branch=gh-pages --cleanup"
  },
  "devDependencies": {
    "nuxt-windicss": "^2.0.1",
    "nuxt3": "latest",
    "push-dir": "^0.4.1"
  },
  "dependencies": {
    "daisyui": "^1.14.5"
  }
}
