{
  "private": true,
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "start": "node .output/server/index.mjs",
    "deploy": "push-dir --dir='.output/public' --branch=gh-pages --cleanup"
  },
  "devDependencies": {
    "nuxt3": "latest",
    "nuxt-windicss": "^2.0.1",
    "push-dir": "^0.4.1"
  },
  "dependencies": {
    "daisyui": "^1.14.5"
  }
}
